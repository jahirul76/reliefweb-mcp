# To register in Cloudera AI studio

```
{
  "mcpServers": {
    "reliefweb-mcp-server": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/jahirul76/reliefweb-mcp.git",
        "run-server"
      ],
      "env": {
        "appname": "placeholder"        
      }
    }
  }
}
```